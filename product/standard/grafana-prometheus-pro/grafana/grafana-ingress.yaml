apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: grafana-basicservice
  name: grafana-basicservice
  namespace: monitoring
spec:
  rules:
  - host: monitor-basicservice.inc-inc.com
    http:
      paths:
      - backend:
          serviceName: grafana-basicservice
          servicePort: 3000
        path: /

