apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-alertmanager
  namespace: monitoring 
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi

