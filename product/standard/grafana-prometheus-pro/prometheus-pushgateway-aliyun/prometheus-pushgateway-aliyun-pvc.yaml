apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-metrics-pushgateway-aliyun-promethues
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "metrics-pushgateway-aliyun-promethues"
  resources:
    requests:
      storage: 100Gi
